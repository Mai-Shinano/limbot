sns = "Misskey"
sns_url = "https://example.net"
# Misskey requires an API token. Automatic OAuth token generation is for Mastodon/Pleroma.
sns_token = "your_misskey_api_token"
# GitHub Models (OpenAI-compatible API)
openai_url = "https://models.github.ai/inference"
# For local testing, use env var MODEL_TOKEN instead of writing secrets here.
# openai_token = "github_pat_..."
# Alias for openai_token. Either value works.
# model_token = "github_pat_..."
openai_model = "openai/gpt-4.1-mini"
master_acct = "your_acct"
memory_file = "memory.json"
instruction = """instruction to an LLM goes here..."""
